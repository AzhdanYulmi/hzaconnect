# Dev/CI only — exposes /dev/host.html (the casino host-page fixture) for
# Playwright to drive against the production-shaped nginx stack.
location = /dev/host.html {
  alias /srv/fixtures/host.html;
  add_header Cache-Control "no-store";
}
